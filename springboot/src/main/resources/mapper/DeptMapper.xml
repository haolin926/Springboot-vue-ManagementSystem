<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springbootdemo.mapper.DeptMapper">

    <update id="update">
        update department
        <set>
            <if test="numEmp != null'">
                num_emp= #{numEmp},
            </if>
        </set>
        where dept_id = #{deptId}
    </update>
    
<!--    <select id="list">-->
<!--        SELECT department.dept_name,department.dept_id,COUNT(employee.id)-->
<!--            FROM department JOIN employee USING(dept_id)-->
<!--            <where>-->
<!--                <if test="search != null and search != ''">name like CONCAT('%' + #{search} +'%')</if>-->
<!--            </where>-->
<!--            GROUP BY department.dept_id,department.dept_name-->
<!--    </select>-->
</mapper>